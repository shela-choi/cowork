{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/notion/:path*",
      "destination": "/api/notion/:path*"
    }
  ]
}
